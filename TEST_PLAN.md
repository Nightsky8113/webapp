# テスト計画とバグ修正記録

## 修正済みのバグ

### 1. Router.js のパス解析の問題
- **問題**: `pathWithQuery.split('?')[0]`が冗長だった
- **修正**: `hash.split('?')[0]`に直接代入

### 2. StoreDetailPage の userLocation nullチェック不足
- **問題**: `userLocation`がnullの場合にエラーが発生
- **修正**: `userLocation`の存在チェックを追加し、距離が計算できない場合は'-'を表示

### 3. Supabase 環境変数未設定時の処理
- **問題**: 環境変数がない場合、`createClient`にundefinedが渡される
- **修正**: デフォルト値を設定し、ダミーオブジェクトを返すように修正

### 4. テンプレート読み込みエラーハンドリング
- **問題**: エラー時に空文字列を返し、原因がわかりにくい
- **修正**: エラーを再スローし、呼び出し側でフォールバック処理ができるように修正

### 5. ルーターパラメータの型変換
- **問題**: URLパラメータが常に文字列として扱われる
- **修正**: 数値に変換できる場合は数値に変換

## テストケース

### 基本動作テスト

#### 1. ホームページ (/home)
- [ ] ページが正常に読み込まれる
- [ ] 「商品から探す」ボタンが表示される
- [ ] 「位置情報から探す」ボタンが表示される
- [ ] 今日更新されたチラシセクションが表示される（データがある場合）
- [ ] 今日更新されたチラシがない場合、適切なメッセージが表示される

#### 2. ジャンルページ (/genre)
- [ ] ページが正常に読み込まれる
- [ ] ジャンル一覧が表示される
- [ ] 商品検索入力欄が表示される
- [ ] 位置情報が取得できる場合、URLパラメータに含まれる

#### 3. 店舗一覧ページ (/stores)
- [ ] 位置情報がない場合、エラーメッセージが表示される
- [ ] 位置情報がある場合、近い順に店舗が表示される
- [ ] 最大6件の店舗が表示される
- [ ] 店舗カードがクリック可能

#### 4. ジャンル別店舗ページ (/genre/:genreId/stores)
- [ ] ジャンルIDが無効な場合、エラーメッセージが表示される
- [ ] 位置情報がない場合、エラーメッセージが表示される
- [ ] 5km以内の店舗が表示される
- [ ] 地図が表示される
- [ ] 店舗マーカーが表示される

#### 5. 店舗詳細ページ (/store/:storeId)
- [ ] 無効な店舗IDの場合、エラーメッセージが表示される
- [ ] 店舗情報が正常に表示される
- [ ] 位置情報がない場合でもページが表示される（距離は'-'）
- [ ] チラシ画像が表示される
- [ ] お気に入りボタンが機能する

#### 6. 検索結果ページ (/search)
- [ ] 検索クエリがない場合の処理
- [ ] 検索結果が表示される
- [ ] フィルターが機能する
- [ ] ソートが機能する

### エッジケーステスト

#### 1. Supabase接続エラー
- [ ] 環境変数がない場合、アプリが起動する
- [ ] データ取得エラー時に適切なフォールバックが動作する

#### 2. テンプレート読み込みエラー
- [ ] テンプレートファイルが見つからない場合、フォールバックHTMLが表示される

#### 3. 位置情報取得エラー
- [ ] 位置情報が拒否された場合、適切なエラーメッセージが表示される
- [ ] Geolocation APIが利用できない場合の処理

#### 4. 無効なURL
- [ ] 存在しないルートにアクセスした場合、ホームページにリダイレクトされる
- [ ] 無効なパラメータが渡された場合の処理

### ブラウザテスト

#### 対応ブラウザ
- [ ] Chrome (最新版)
- [ ] Firefox (最新版)
- [ ] Safari (最新版)
- [ ] Edge (最新版)

#### モバイル
- [ ] iOS Safari
- [ ] Android Chrome

## 実行手順

1. 開発サーバーを起動: `npm run dev`
2. ブラウザで `http://localhost:5173` を開く
3. 各テストケースを順番に実行
4. コンソールでエラーを確認
5. 期待される動作と実際の動作を比較

