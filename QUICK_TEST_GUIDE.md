# クイックテストガイド

## まず確認すべきこと

### 1. 開発サーバーを起動
```bash
npm run dev
```

### 2. ブラウザで開く
`http://localhost:5173` を開く

### 3. コンソールを開く
F12キーまたは右クリック → 「検証」 → 「Console」タブ

## 確認すべき動作

### ✅ ホームページ (/home)
1. ページが正常に表示されるか
2. コンソールにエラーが出ていないか
3. 「商品から探す」と「位置情報から探す」ボタンが表示されるか
4. 今日更新されたチラシセクションが表示されるか（または「今日更新されたチラシはありません」メッセージ）

### ⚠️ よくある問題と対処法

#### 問題1: ページが白いまま
- **原因**: JavaScriptエラーまたはテンプレート読み込みエラー
- **確認**: コンソールでエラーメッセージを確認
- **対処**: 
  - テンプレートファイルが正しい場所にあるか確認
  - `src/templates/pages/home-page.html` が存在するか確認

#### 問題2: Supabase接続エラー
- **原因**: 環境変数が設定されていない
- **確認**: コンソールに「⚠️ Supabase環境変数が設定されていません」が表示される
- **対処**: 
  - `.env`ファイルを作成し、`VITE_SUPABASE_URL`と`VITE_SUPABASE_ANON_KEY`を設定
  - または、モックデータを使用する場合はこのままでも動作するはず

#### 問題3: テンプレート読み込みエラー
- **原因**: テンプレートファイルが見つからない
- **確認**: コンソールに「Template load error」が表示される
- **対処**: 
  - フォールバックHTMLが表示されるはず
  - テンプレートファイルのパスを確認

#### 問題4: 位置情報取得エラー
- **原因**: ブラウザが位置情報を許可していない
- **確認**: 位置情報が必要なページでエラーメッセージが表示される
- **対処**: 
  - ブラウザの設定で位置情報を許可する
  - または、URLパラメータで位置情報を指定: `#/stores?lat=35.6812&lng=139.7671`

## テスト手順

1. **ホームページのテスト**
   - URL: `http://localhost:5173/#/home`
   - ページが正常に表示されることを確認
   - コンソールにエラーがないことを確認

2. **ジャンルページのテスト**
   - 「商品から探す」ボタンをクリック
   - 位置情報の許可を求めるダイアログが表示される
   - 許可するとジャンルページに遷移する
   - ジャンル一覧が表示されることを確認

3. **店舗一覧ページのテスト**
   - 「位置情報から探す」ボタンをクリック
   - 位置情報の許可を求めるダイアログが表示される
   - 許可すると店舗一覧ページに遷移する
   - 近い順に店舗が表示されることを確認

## デバッグ情報

### コンソールに表示される情報
- `🚀 アプリを初期化中...` - アプリが起動している
- `✅ アプリの初期化完了` - アプリが正常に起動した
- `✅ Supabase接続成功` - Supabaseが正常に接続された
- `⚠️ Supabase環境変数が設定されていません` - 環境変数が設定されていない（動作は継続）
- `❌` で始まるメッセージ - エラーが発生している

### よくあるエラー
- `Template load error` - テンプレートファイルが見つからない
- `ページ描画エラー` - ページの描画中にエラーが発生
- `位置情報の取得に失敗` - 位置情報が取得できない

## 次のステップ

すべての基本動作が確認できたら、`TEST_PLAN.md`の詳細なテストケースを実行してください。

