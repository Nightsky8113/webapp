# 無料で実装できる項目

## ✅ 実装可能な項目（コスト0円）

### 1. 店舗データの集計処理（API不要）⭐️ **最優先**

**概要**: 既存の商品データから`summary_best_item_name`、`summary_best_item_price`、`summary_best_item_id`を自動計算

**実装方法**:
- PostgreSQLの関数・トリガーで実装（Supabase無料枠内）
- 商品データが更新されたときに自動的に`stores`テーブルの`summary_*`フィールドを更新

**メリット**:
- 外部API不要（完全無料）
- リアルタイムに最新の最安価商品を反映
- パフォーマンスが良い（データベース内で完結）

---

### 2. お気に入り機能のSupabase連携 ⭐️ **高優先度**

**現状**: LocalStorageで実装済み

**実装内容**:
- Supabaseの`favorites`テーブルを使用
- 複数デバイス間で同期可能
- LocalStorageとSupabaseのハイブリッド（フォールバック対応）

**メリット**:
- Supabase無料枠内
- データの永続化
- 将来的な認証機能への拡張が容易

---

### 3. 検索履歴のSupabase連携 ⭐️ **高優先度**

**現状**: LocalStorageで実装済み

**実装内容**:
- Supabaseの`search_history`テーブルを使用
- 検索パターンの分析が可能（将来的に）

**メリット**:
- Supabase無料枠内
- データの永続化

---

### 4. 環境変数設定ファイル

**実装内容**:
- `.env.example`ファイルの作成
- 必要な環境変数の一覧と説明を記載

**メリット**:
- 他の開発者が環境構築しやすい
- ドキュメントとしても機能

---

### 5. Vercelデプロイ設定

**実装内容**:
- `vercel.json`の設定（必要に応じて）
- デプロイ手順のドキュメント

**メリット**:
- Vercel無料枠あり（個人利用）
- 簡単にデプロイ可能

---

### 6. Supabase本番環境設定

**実装内容**:
- 本番環境のSupabaseプロジェクト作成
- マイグレーションの実行
- シードデータの投入

**メリット**:
- Supabase無料枠あり
- 本番環境での動作確認が可能

---

### 7. データベース関数・トリガーの最適化

**実装内容**:
- 商品価格更新時に自動で`summary_best_item`を更新するトリガー
- 集計処理を自動化するPostgreSQL関数

**メリット**:
- 完全無料（PostgreSQL標準機能）
- 自動化により手動更新が不要

---

### 8. Supabase Storage設定（画像アップロード基盤）

**実装内容**:
- `flyer-images`バケットの作成
- 公開読み取りポリシーの設定
- 画像アップロードUI（管理者用）

**メリット**:
- Supabase無料枠内（1GBまで無料）
- 将来的なOCR処理への準備

---

### 9. テストフレームワークの導入

**実装内容**:
- Vitestの導入（Viteに最適化されたテストフレームワーク）
- ユーティリティ関数のユニットテスト

**メリット**:
- 完全無料
- 品質保証
- リファクタリング時の安全性

---

### 10. ドキュメント整備

**実装内容**:
- APIドキュメント
- 開発者向けドキュメント
- 運用マニュアル

**メリット**:
- 完全無料
- 保守性向上
- チーム開発が容易に

---

## 🎯 実装順序（推奨）

### Phase 1: データ集計の自動化（最優先）
1. PostgreSQL関数で`summary_best_item`を自動計算
2. 商品更新時のトリガーで自動更新

### Phase 2: データ永続化の改善
3. お気に入り機能のSupabase連携
4. 検索履歴のSupabase連携

### Phase 3: 環境構築・デプロイ準備
5. `.env.example`の作成
6. Vercelデプロイ設定
7. Supabase本番環境設定

### Phase 4: 機能拡張
8. Supabase Storage設定
9. 画像アップロードUI（管理者用）

### Phase 5: 品質向上
10. テストフレームワーク導入
11. ドキュメント整備

---

## 💡 実装時の注意点

### Supabase無料枠の制限
- **データベース**: 500MB
- **Storage**: 1GB
- **Bandwidth**: 5GB/月
- **Edge Functions**: 50万リクエスト/月

→ 小規模な運用なら十分に無料枠内で運用可能

### 実装の優先順位
1. **データ集計処理** - 既存データから自動計算できるので、API不要で実装可能
2. **お気に入り・検索履歴** - ユーザー体験の向上
3. **デプロイ設定** - 本番環境で動作確認

---

## 📊 実装効果

### 実装後の状態
- ✅ 店舗データの集計が自動化される
- ✅ ユーザーデータが永続化される
- ✅ 本番環境で動作確認できる
- ✅ 将来的な機能拡張の基盤が整う

### コストがかかる機能は後回し
- ⏸️ Google Maps Directions API（徒歩時間計算）
- ⏸️ Google Cloud Vision API（OCR処理）
- ⏸️ OpenAI API（構造化処理）

→ これらの機能は必要になったときに実装すればOK

